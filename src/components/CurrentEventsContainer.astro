---
import '../styles/global.css';
import CurrentEventCard from './CurrentEventCard.astro';
import { getCollection } from 'astro:content';

const events: any[] = await getCollection('events' as any);
---

<div id="card-container"
class="w-full h-min grid lg:grid-cols-4 grid-cols-3
bg-tan border-1 md:border-2 border-black rounded-2xl lg:rounded-[20px]
lg:px-36 px-3 lg:py-10 py-3 gap-x-0 lg:gap-y-4 gap-y-2">
    {events.map(event => (
        <CurrentEventCard text={event.data.text} link={event.data.link} graphic={event.data.graphic}/>
    ))}
</div>